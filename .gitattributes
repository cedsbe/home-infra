# Git-crypt configuration for secrets management
# This file defines which files should be encrypted with git-crypt

# Terraform secrets files
*.secrets.auto.tfvars filter=git-crypt diff=git-crypt
backend.config filter=git-crypt diff=git-crypt

# All environment files
**/.env filter=git-crypt diff=git-crypt

# Any other sensitive files
kubeconfig filter=git-crypt diff=git-crypt
*.key filter=git-crypt diff=git-crypt
*.pem filter=git-crypt diff=git-crypt

# Keep template files unencrypted (they contain no secrets)
*.template !filter !diff
