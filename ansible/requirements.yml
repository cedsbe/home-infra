---
# Ansible Collections Requirements
collections:
  - name: ansible.posix
    version: ">=2.1.0"

  # Windows Management
  - name: ansible.windows
    version: ">=3.3.0"

  - name: community.docker
    version: ">=5.0.4"

  # Community General Collection (includes many useful modules)
  - name: community.general
    version: ">=12.1.0"

  # Community Windows Collection
  - name: community.windows
    version: ">=3.1.0"

# Ansible Roles Requirements (optional)
roles: []
  # Example role from Ansible Galaxy:
  # - name: geerlingguy.docker
  #   version: "7.0.0"
