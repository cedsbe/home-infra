---
# Default variables for base-windows role

# System configuration
timezone: UTC
ntp_servers:
  - 0.pool.ntp.org
  - 1.pool.ntp.org
  - 2.pool.ntp.org

# Windows Firewall
windows_firewall_enabled: true

# PowerShell
powershell_execution_policy: RemoteSigned

# Windows Update
windows_auto_update: false
windows_update_categories:
  - CriticalUpdates
  - SecurityUpdates

# Windows Features
windows_features: []
