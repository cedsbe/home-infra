apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: plex
spec:
  type: ClusterIP
  selector:
    app: plex
  ports:
    - name: http
      port: 32400
      targetPort: plex-tcp
      protocol: TCP
